显示缩略(1666,11): df.shape
show basic statistics: df.describe()
show data overview: df.values

创建虚拟环境: python -m venv venv

设置环境变量: pyinstaller -F main.py

打包: pyinstaller -F main.py --noconsole --onefile

Nginx:
    启动: sudo nginx
    重新加载配置: sudo nginx -s reload
    重启: sudo nginx -s reopen
    停止: sudo nginx -s stop
    退出: sudo nginx -s quit
    查看版本，以及配置文件地址: sudo nginx -V
    查看版本: sudo nginx -v
    指定配置文件: sudo nginx -c filename
    帮助: sudo nginx -h

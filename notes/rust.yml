show version: rustup --version, rustc --version

编译: rustc -o output_filename filename.rs

生成库文件: rustc --create-type lib filename.rs

new workspace: cargo new <name> && cd <name>

web:

安装当前程序到shell:
    当前目录: cargo install --path .
    github: cargo install --git https://github.com/xxx/mytool
